export CCACHE_DIR=/home/pete/gustafson.github.io/ccache
bundle install --path vendor/bundle
bundle exec jekyll serve --livereload ## --incremental (incremental will update changed files and new files but not config files)
